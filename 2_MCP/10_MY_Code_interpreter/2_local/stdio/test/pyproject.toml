[project]
name = "mcp-local-python-executor-stdio-test"
version = "1.0.0"
description = "Test client for MCP Local Python Executor (STDIO)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.3.1",
]

[tool.uv]
dev-dependencies = []
