# MCP (Model Context Protocol) - core requirement
mcp>=1.0.0

# HTTP server dependencies
uvicorn>=0.27.0
starlette>=0.36.0
sse-starlette>=1.8.0

# For async operations (if not using Python 3.11+)
asyncio>=3.4.3

# For better process management and isolation
psutil>=5.9.0

# For JSON schema validation (useful for validating input)
jsonschema>=4.17.0

# For logging and debugging
structlog>=23.1.0

# For timeout management
timeout-decorator>=0.5.0

# For better error handling and traceback formatting
better-exceptions>=0.3.3

# If you want to support virtual environment creation
virtualenv>=20.24.0

# For code analysis and safety checks (optional)
bandit>=1.7.5
pylint>=3.0.0
